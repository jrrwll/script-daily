rootProject.name = 'script-daily'

def projects = [
        'common',
        'text-find',
        'send-mail',
        'mongo-op',
        'es-op',
        'fs-op',
        'json-op',
        'jdbc-op',
        'eval-op'
]

projects.forEach {
    include it
}

include 'jdbc-op-aot'
project(':jdbc-op-aot').projectDir = new File(rootDir, 'jdbc-op/aot')
