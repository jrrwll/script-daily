rootProject.name = 'script-daily'

def projects = [
        'common',
        'text-find',
        'send-mail',
        'mongo-op',
        'es-op',
        'fs-op',
        'json-op',
        'jdbc-op',
        'eval-op'
]

projects.forEach {
    include it
}
